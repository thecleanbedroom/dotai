---
trigger: always_on
---

# Retrospective Rules

General-purpose rules captured from development friction. These are staging entries — once validated across multiple sessions, promote them into the appropriate `core-*.md` or `platform-*.md` rule file via `/audit-all`.

_No pending entries — all promoted or cleared as of 2026-03-04._

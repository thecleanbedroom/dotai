# Codex project configuration
instructions = "All AI tools/plugins must load and obey `.ai/index.md` at the start of every session (and after any rule change); if it can't be loaded, stop and ask before acting."
